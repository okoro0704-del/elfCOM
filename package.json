{
  "name": "elfcom",
  "private": true,
  "version": "0.1.0",
  "description": "ElfCom — sovereign communication platform (consumer chat, ecosystem backbone, omnichannel inbox, BaaS)",
  "workspaces": [
    "apps/*",
    "packages/*",
    "adapters/*"
  ],
  "scripts": {
    "build": "npm run build -w @elfcom/contract && npm run build -w @elfcom/crypto && npm run build -w @elfcom/connectors-core && npm run build -w @elfcom/connectors && npm run build -w @elfcom/lifeos-adapter && npm run build -w @elfcom/node && npm run build -w @elfcom/console",
    "dev": "npm run dev -w @elfcom/node",
    "dev:console": "npm run dev -w @elfcom/console",
    "start": "npm run start -w @elfcom/node",
    "test": "npm run test -w @elfcom/crypto && npm run test -w @elfcom/connectors && npm run test -w @elfcom/node"
  },
  "engines": {
    "node": ">=20"
  }
}

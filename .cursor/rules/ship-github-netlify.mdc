---
description: After every production cycle, commit, push GitHub, deploy Netlify
alwaysApply: true
---

# Production cycle: GitHub + Netlify

After completing a **production cycle** (feature/fix ready to ship, or user asks to ship):

1. **Commit** all relevant production changes (never `.env`, credentials, `.netlify/` local state, or `netlify-sites.json`).
2. **Push** to `origin` on the current branch (`git push -u origin HEAD` if needed).
3. **Deploy Netlify** production for site `elfcom` (`a0af2b75-2628-4292-9776-63e9be35f2e3`):
   - Build: `npm run build -w @elfcom/web`
   - Pack a case-safe zip (Windows zips break Netlify asset paths otherwise):
     `npm run pack:netlify -w @elfcom/web`
     → writes `%TEMP%\elfcom-netlify-safe.zip` with lowercase `/`-separated paths
   - Upload + publish via Netlify API (CLI often fails monorepo / auth on this machine):
     `POST https://api.netlify.com/api/v1/sites/a0af2b75-2628-4292-9776-63e9be35f2e3/deploys`
     with `Authorization: Bearer $NETLIFY_AUTH_TOKEN` and `Content-Type: application/zip`
   - Verify live `/assets/*.js` returns 200 and contains expected feature strings before reporting done
4. Report the live URL: https://elfcom.netlify.app

Do this **without waiting for a separate ask** when the cycle is clearly done, unless the user said not to ship yet.
